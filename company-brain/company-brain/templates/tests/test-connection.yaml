apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "company-brain.fullname" . }}-test-connection"
  labels:
    {{- include "company-brain.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "company-brain.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
