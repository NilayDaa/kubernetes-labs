apiVersion: v1
kind: Service
metadata:
  name: {{ include "company-brain.fullname" . }}
  labels:
    {{- include "company-brain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "company-brain.selectorLabels" . | nindent 4 }}
